function myFunction():void{
  //some code
  trace(myArray[myArray.length]);
  //some code
}

//This will throw an error if myArray is empty because you are trying to access an index that does not exist.